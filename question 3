Question 3:
Show first name of patients that start with the letter 'C'

Solution:
SELECT first_name FROM patients
where SUBSTR(first_name,1,1)='C';

//OR

SELECT first_name FROM patients
where SUBSTRING(first_name,1,1)='C';

//HERE (first_name,1,1)=>(name of column, position of character from where substring start, length of substring)
